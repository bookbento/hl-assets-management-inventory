{
  "name": "it-assets-inventory",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run backend:dev & pnpm run frontend:dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint",
    "backend:dev": "pnpm --dir backend start:dev",
    "frontend:dev": "pnpm --dir frontend dev"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}
